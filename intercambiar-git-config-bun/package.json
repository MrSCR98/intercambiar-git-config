{
  "name": "intercambiar-git-config",
  "version": "1.2.0",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
    "scripts": {
    "dev": "bun run intercambiar-git-config.ts",
    "test": "bun test",
    "exe": "bun build intercambiar-git-config.ts --compile --outfile exe/intercambiar-git-config.exe --minify --bytecode"
  }
}
